name: "AI PR Reviewer (Disabled: Deprecated Action)"

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Skip Review
        run: |
          echo "The coderabbitai/openai-pr-reviewer action is deprecated."
          echo "Waiting for Phase 2 Local LLM migration."
