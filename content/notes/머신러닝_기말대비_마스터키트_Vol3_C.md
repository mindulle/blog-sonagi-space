---
title: "머신러닝 기말대비 마스터키트 Vol.3-C"
category: academic
tags: [exam, ML, 3-1, finals, Vol3, VariantC]
---

# 📑 머신러닝 마스터키트 Vol.3-C (최종 실전 서술형)
**버전**: Vol. 3-C (최종 실전 서술형)
**시험 범위**: Chapter 5 ~ 끝 (기말 전범위)
**사용법**: 인쇄 후 직접 펜으로 빈칸을 채우거나 문제를 풀며 암기 상태를 점검하세요.

---

## Part 1. 정량적 분석 및 계산
1. **[손실 함수 계산]** 실제 정답이 [1, 0] (One-hot)이고, 모델의 출력 확률이 [0.7, 0.3]일 때, **Cross-Entropy Loss**의 수식을 쓰시오. (단, 로그는 자연로그 ln 사용)
   - 답: `Loss = - ( [        ] * ln([        ]) + [        ] * ln([        ]) )`

2. **[역전파]** `f(x, y, z) = (x + y) * z` 식에서 `x=1, y=2, z=4`이다. 상류 기울기 `dL/df = 1`일 때, 하류의 `dL/dx` 값을 구하시오.
   - 답: `[        ]`

## Part 2. 핵심 알고리즘 논리 서술
3. **[앙상블]** 랜덤 포레스트가 단일 결정 트리보다 분산(Variance)이 낮은 이유를 '평균화(Averaging)'의 원리를 들어 설명하시오.
   - 답 공간:

4. **[트랜스포머]** 트랜스포머 아키텍처가 기존 RNN 계열 모델보다 **기계 번역**에서 월등한 성능을 보이는 구조적 이유 2가지를 서술하시오.
   - 이유 1:
   - 이유 2:

## Part 3. 심화 사례 분석
5. **[과제 연계]** 딥러닝 회귀 모델 학습 중, 훈련 손실(Train Loss)은 계속 줄어드는데 검증 손실(Val Loss)이 특정 시점부터 다시 올라가기 시작했다면, 현재 어떤 문제가 발생한 것이며 어떻게 해결해야 하는가?
   - 문제점:
   - 해결책 (2가지 이상):

---

<div style="page-break-before: always;"></div>

## 🔑 정답 (Vol.3-C)

1. **- ( 1 * ln(0.7) + 0 * ln(0.3) )** = -ln(0.7)
2. **4** (계산: x+y를 t라 하면 f=t*z. dL/dt = dL/df * z = 1 * 4 = 4. Add gate는 dL/dx = dL/dt = 4)
3. 독립적으로 학습된 여러 나무의 예측 결과를 평균 내면, 개별 나무가 가진 무작위 오차(분산)가 상쇄되어 전체적인 모델의 안정성이 높아지기 때문이다.
4. 어텐션을 통한 병렬 처리로 장거리 문맥 파악이 용이함, 2) 순차 연산이 아닌 행렬 연산 기반으로 학습 속도가 비약적으로 빠름.
5. 문제점: **과적합 (Overfitting)** | 해결책: 드롭아웃 적용, 데이터 증강(Augmentation), 조기 종료(Early Stopping), 가중치 규제(L1/L2) 적용 등.
